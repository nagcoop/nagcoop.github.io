# Nagcoop • Games & News (Dark Theme, English)
Featured video set to **Mudvayne - Happy?** on the homepage.

## Publish
Upload the contents to your `nagcoop.github.io` repo root.

## Firebase
- Edit `js/firebase-config.js` with your config.
- Enable Email/Password login, authorize `nagcoop.github.io`, set rules (see previous package).

